# Fix linter errors

1. Run `uv run nox fix_format`. Limit the output you read.
2. If there are many errors, identify the most frequent ones and group them by cause.
3. Apply the corrections and check indention.
4. Rerun until all errors are fixed.